---

name: DevOps Project Pipeline

on:
  push:
    branches:
      - main

jobs:
  build_and_push:
    runs-on:  ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set Short SHA
        id: set_sha
        run:  |
          echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build Docker image
        run:  |
          docker build -t mosesade/devops:${{ steps.set_sha.outputs.SHORT_SHA }} .

      - name: Login to Dockerhub
        run:  |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      
      - name: Push to Dockerhub
        run:  |
          docker push mosesade/devops:${{ steps.set_sha.outputs.SHORT_SHA }}

  test:
    needs: build_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Login to Dockerhub
        run:  |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          
      - name: Pull the built image
        run: |
          echo "Pulling built image: mosesade/devops:${{ needs.build_and_push.outputs.SHORT_SHA }}"
          docker pull mosesade/devops:${{ needs.build_and_push.outputs.SHORT_SHA }}

      - name: Test container
        run: |
          echo "Running test....."
          docker run --rm mosesade/devops:${{ needs.build_and_push.outputs.SHORT_SHA }}  python -m pytest tests/ -v